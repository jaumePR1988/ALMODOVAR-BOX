rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
    
    // More specific rules for production:
    // match /exercise-images/{imageId} {
    //   allow read: if true;
    //   allow write: if request.auth != null && request.auth.token.role == 'coach';
    // }
  }
}
